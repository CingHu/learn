
根据需求confnet程序需要在server 2003与server 2008上允许,由于2003版本太低, 所以不能使用vs2008编译, 所以这里使用了vc6.0来编译程序, 编写程序时用到了iphlpapi库,但是vc6.0没有提供这个库,我这里安装了windows开发SDK,来解决了这个问题,当然你也可以自己下载.lib与.h文件来解决.

1. 首先需要新创建一个confnet的工程，这里最好是以confnet命名，如果不是这个名字，编译完成后需要将.exe改为confnet.exe
　 新建一个"Win32 Console Application"

2.导入文件
　将.cpp与.h文件导入到新建的工程里

3.编译
  按F7编译，编译完成后会在工程下的Debug文件夹下生成一个.exe的可执行文件

